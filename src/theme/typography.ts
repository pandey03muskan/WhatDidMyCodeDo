/**
 * Typography configuration
 * Uses CSS variables for Poppins and Inter fonts
 */
export const typography = {
  fontFamily: 'var(--font-poppins), var(--font-inter), sans-serif',
  h1: {
    fontFamily: 'var(--font-poppins), var(--font-inter), sans-serif',
    fontWeight: 700,
    fontSize: '2.5rem',
    lineHeight: 1.2,
  },
  h2: {
    fontFamily: 'var(--font-poppins), var(--font-inter), sans-serif',
    fontWeight: 700,
    fontSize: '2rem',
    lineHeight: 1.3,
  },
  h3: {
    fontFamily: 'var(--font-poppins), var(--font-inter), sans-serif',
    fontWeight: 600,
    fontSize: '1.75rem',
    lineHeight: 1.4,
  },
  h4: {
    fontFamily: 'var(--font-poppins), var(--font-inter), sans-serif',
    fontWeight: 600,
    fontSize: '1.5rem',
    lineHeight: 1.4,
  },
  h5: {
    fontFamily: 'var(--font-poppins), var(--font-inter), sans-serif',
    fontWeight: 600,
    fontSize: '1.25rem',
    lineHeight: 1.5,
  },
  h6: {
    fontFamily: 'var(--font-poppins), var(--font-inter), sans-serif',
    fontWeight: 600,
    fontSize: '1.125rem',
    lineHeight: 1.5,
  },
  body1: {
    fontFamily: 'var(--font-inter), sans-serif',
    fontWeight: 400,
    fontSize: '1rem',
    lineHeight: 1.5,
  },
  body2: {
    fontFamily: 'var(--font-inter), sans-serif',
    fontWeight: 400,
    fontSize: '0.875rem',
    lineHeight: 1.5,
  },
  button: {
    fontFamily: 'var(--font-poppins), var(--font-inter), sans-serif',
    fontWeight: 600,
    textTransform: 'none',
  },
  caption: {
    fontFamily: 'var(--font-inter), sans-serif',
    fontWeight: 400,
    fontSize: '0.75rem',
    lineHeight: 1.4,
  },
};
